#include<iostream>
using namespace std;

int main()
{
    int divisior, dividened, quotient, remainder

    cout<<"Enter dividened";
    cin >> dividened;

    cout<<"Enter divisor";
    cin >> divisor;

    quotient = dividended / divisor;
    remainder = dividended % divisor;

    cout<< "Quotient" = << "quotent" << endl;
    cout<< "Remainder" = << "remainder";

return 0

}
