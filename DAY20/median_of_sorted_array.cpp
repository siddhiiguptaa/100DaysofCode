#include <iostream>
#include <bits/stdc++.h>
using namespace std;
int main(){
int n;
int m;
int arr1[n];
cin >> arr[n];
int arr2[m];
cin >> arr[n];
vector<int> sum;
sum[m+n] = arr1[n]+arr2[m];
sort(sum.begin(), sum.end());
if((m+n) % 2 != 0){
    cout << (m+n )/2;
}
    cout << ((((m+n))-1)+(m+n))/2;
}
